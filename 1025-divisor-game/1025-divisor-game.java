class Solution {
    public boolean divisorGame(int n) {
        
        int ac=0, bc=0;
        // int[] dp = new int[n+1];
        // dp[1] = 0;
        // for(int i=2; i<=n; i++) {
        //     dp[i] = dp[i-1]+1;
        // }
        
        if(n%2==0)
            return true;
        return false;
    }
}